import { Head } from "@inertiajs/react";

export default function Contact({ title }) {
    return (
        <div>
            <Head title={title} />

            <h1>Contact</h1>
        </div>
    );
}
